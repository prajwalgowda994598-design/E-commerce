const jwt=require('jsonwebtoken'),User=require('../models/User');exports.protect=async(req,res,next)=>{try{let t=req.headers.authorization?.split(' ')[1];if(!t)return res.status(401).json({message:'Not authorized'});req.user=await User.findById(jwt.verify(t,process.env.JWT_SECRET).id);if(!req.user)return res.status(401).json({message:'Not authorized'});next()}catch(e){res.status(401).json({message:'Invalid token'})}};exports.admin=(req,res,next)=>req.user?.isAdmin?next():res.status(403).json({message:'Admin access required'});
